<div class="row">
	<h3> Grupos </h3>
</div>

<div class="row">
	<table class="bordered highlight responsive-table">
        <thead>
          <tr>
              <th>Nombre</th>
              <th>Información</th>
              <th>Editar</th>
          </tr>
        </thead>

        <tbody>
        	<% @admins.each do |a| %>
        	<tr>
				<td></td>
				<td></td>
				<td><%= check_box_tag(:editarGrupo) %></td>
			</tr>
			<%end%>
        </tbody>
    </table>
</div>

<div class="row">
	<%= will_paginate @admins %>
</div>

<div class="row center">
  <%= link_to "Agregar Grupos", "/admin/agregarGrupos" , :class => 'waves-effect waves-light btn'%>
</div>